{
  "name": "server",
  "version": "1.0.0",
  "description": "socketIO minimal client-server",
  "main": "index.js",
  "scripts": {
    "mypy": "mypy --config-file ./mypy/mypy.ini ./src/analysis/ ./src/variation/ ./src/synthesis/ ./src/util/ > ./mypy/mypy.log", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "file-saver": "^2.0.2",
    "live-server": "^1.2.1",
    "socketio": "^1.0.0"
  }
}
